export interface Item {
    id: number;
    name: string;
    description: string;
    // attribute1?: string;
    // attribute2?: number;
    // attribute3?: boolean;
    // Add more attributes as needed during the test
}
